import { sharedUtilAppConfig } from './shared-util-app-config';

describe('sharedUtilAppConfig', () => {
  it('should work', () => {
    expect(sharedUtilAppConfig()).toEqual('shared-util-app-config');
  });
});
